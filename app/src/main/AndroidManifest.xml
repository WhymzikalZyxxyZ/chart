<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="CHART"
        android:theme="@android:style/Theme.Material.NoActionBar"
        android:allowBackup="false"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          AppAuth's RedirectUriReceiverActivity is added automatically by its
          Gradle plugin manifest merger, keyed off appAuthRedirectScheme in
          defaultConfig. No SMART launch flow is wired up yet — see
          docs/adr/002-fhir-source-and-auth.md — this is the reserved surface
          for it.
        -->

    </application>

</manifest>
